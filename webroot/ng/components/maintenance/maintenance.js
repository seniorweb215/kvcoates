'use strict';
angular.module('digitalMedia.maintenance', [])
        .controller("maintenanceController", ["$rootScope", "$scope", function ($rootScope, $scope) {
                $rootScope.title = "Maintenance";

                var init = function () {
                    
                };
                init();

            }]);